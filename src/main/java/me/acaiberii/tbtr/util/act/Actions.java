package me.acaiberii.tbtr.util.act;

public class Actions {
    public static String formatError(String action) {
        return "an error occured while " + action + ". Check the console for more info.";
    }
}
